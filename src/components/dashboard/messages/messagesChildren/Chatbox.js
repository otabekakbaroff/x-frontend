import React from 'react'



function ChatBox(){
    return(
        <div className="chat-box" style={{paddingBottom:"25px", paddingTop:"25px", marginTop:"65px", background:"pink", }}>
            {/* {myMessages.map(item=>(
                <div key={Math.floor(Math.random()*99999999)} className={`${item.from === localStorage.getItem('username')}`}>
                    <p>{item.message}</p>
                </div>
            ))} */}
            <div className ="contactprofile" style={{display:"grid"}}>iojiojdfi</div>
        </div>
    )
}

export default ChatBox;
